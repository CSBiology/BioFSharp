language: csharp
#sudo: required
sudo: false
#dist: trusty # Ubuntu 14.04
dotnet : 3.1.101

script:
 - dotnet tool restore
 - dotnet restore build.proj
 - dotnet paket restore
 - dotnet fake build -t Linux

branches:
  only:
  - master
  - developer